## `training/vision/`
**Purpose:** Training and evaluating gate-detection / segmentation models.  
**What lives here:** Dataset code, synthetic data generation, augmentation pipelines, model training scripts, validation scripts, and export tools.  
**How it relates to the rest of the code:** Produces learned perception backends that can replace simple classical detectors while keeping the same runtime interface.
